import React from "react";
import { render } from "react-dom";

const app = (name) => {
    // test

    // return React.createElement("div", {}, "h1");
    return (
        <div>
            <h1>{name}</h1>
        </div>
    )
};
